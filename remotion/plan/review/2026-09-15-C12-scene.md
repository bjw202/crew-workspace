# C-12 관문 판정 — 장면 12 (껐다 켜도, 이어 붙기 resume) · C11 고침 재확인

판정한 때: 2026-09-15. meta 가 S12-0260 · S12-0440 을 열었다. C11 고침 셋은 보고로 받아들임 (상자 부제 15px 넘침은 받아들임).

## S12 판정: **통과** (값 하나 고침 조건)

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | still = 비트 | ✓ | 260: 서버 창 4줄(`^C` · `끄는 중 …` · `serve …` · `듣는 중 …`) · `bootResume` · 읽기 점선 `stopped 가 아닌 줄` · 편지함 `#16 미배달` `#17 미배달` · `starting — 아직 배달하지 않는다` · CLI `켜는 중 starting` · 봇 칩 ⚪ · 판 `켜는 중` · 글 둘 · 자막 2. 440: `pendingInbox (BATCH_LIMIT 20)` · 겉봉투 묶음이 CLI 에 닿는 중 · `SessionStart (resume) ·` · CLI `대기 idle` · 🟢 |
| **2** | 화면 글자 | ✓ | TERM_12 네 줄 = bin/cockpit.js · ENVELOPE_12 (작아서 안 읽히나 04 규칙) |
| 3 | 자막 | ✓ | |
| 4 | 주석 | ✓ | 5줄 |
| 5 | DURATION | ✓ | 480 |
| 6 | 못 옮긴 줄 | 받아들임 | 이름표 y 390 · 창 불투명 바탕 · 줄 높이 1.6 · 같은 자리 이름표 교체 · 나무 걷힘/새 나무 · 봉투 사라짐 · POST 교체 · glow |

## 빗나간 줄 (S13 전에 고친다)

1. (보고 b) CLI 부제 `프로세스 2 · session_id abc…` 가 상태 글자에 밀려 잘린다 → 부제를 **`프로세스 2 · abc…`** 로 (짧게). 440 의 "같음" 강조는 아래 이름표 `session_id abc… (같음)` 로 충분하다.

## meta 결정

(a) `resume: abc…` 칩이 250 에 닿아 260 에 없음 — 받아들임 (0260 에 `켜는 중 starting` 과 부제로 보인다). (c)(d)(e)(f) — 받아들임.

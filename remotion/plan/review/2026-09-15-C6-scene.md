# C-6 관문 판정 — 장면 6 (글 한 번 왕복) · C5 고침 재확인

판정한 때: 2026-09-15. meta 가 S05-0350(고친 뒤) · S06-0400 · S06-0720 을 열었다.

## C5 고침: ✓ (hooks 점선이 기둥 사이 틈으로 · 뿌리 카드 S03/S04)

## S06 판정: **통과** (부품 하나 · 값 둘 고침 조건)

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | still = 비트 | ✓ | 400: 겉봉투 4줄 크게 · 전체 조명 · `pendingInbox` · `wrapChannel` · 칩 `#7` · `#7 → bot · to` · `enqueue #7` · 내 글 · 자막 1. 720: `mcp__cockpit__reply` → MCP 상자(빛) · `onBotMessage` · `#8 bot` · `#7 delivered` · SSE · CLI working · 자막 2 |
| **2** | 화면 글자 | ✓ | ENVELOPE_6 4줄 = text.ts = wrap.js · `wrapChannel — src/envelope/wrap.js:62-68` · `POST /api/rooms/:id/messages` · `mcp__cockpit__reply` |
| 3 | 자막 | ✓ | 2판 문장 |
| 4 | 주석 | ✓ | 3줄 |
| 5 | DURATION | ✓ | 960 |
| 6 | 못 옮긴 줄 | 받아들임 | 화살표 되그리기 · 글 카드 holdAfter false · 봉투 자리 |

## 빗나간 줄 (S07 전에 고친다)

1. **Pillar 부품 (보고 f):** `working` 상태 글자 `일하는 중 working` 이 제목과 한 줄에 안 들어가 두 줄로 접히고, 부제 `cwd …` 가 밀려 CLI 속 첫 줄과 겹친다. 고침: 상태 글자를 **부제 줄 오른쪽 끝**(mono 14, 오른쪽 정렬)에 두고, 제목 띠 높이는 100~160 고정, 제목은 한 줄(넘치면 줄임). S08 · S09 · S11 에서도 쓰이므로 부품에서 고친다.
2. reply 화살표 끝(1118,300)이 MCP 이름표 글자에 닿는다 → 끝 x **1140**. (fetch_history 도 같은 자리 — S07 부터 같이.)
3. 조종석 판 상태 글자(보고 d): CLI 가 `working` 이면 판도 `일하는 중`, `waiting` 이면 `승인 대기` 로 따라간다 (cockpit/web/cockpit.js:11 의 이름). S06 560~740 에 적용. 봇 칩 `(입력 중…)` 은 안 쓴다.

## meta 결정 (보고 a·b·c·e)

(a) 겉봉투 0.3배 600~640 사라짐 — 받아들임. (b) MCP 이름표 frame 0 부터, 끝까지 빛 — 받아들임 (S07 은 glow 없이 시작). (c) 앞 장면의 이름표·점선을 안 넘기는 것 — 그대로. (e) 칩 `+1` 접힘 — 받아들임.

# C-4 관문 판정 — 장면 4 (켜기, `query()` 한 번)

판정한 때: 2026-09-15. 채점표 C-N. meta 가 S04-0300 · S04-0760 을 열었다.

## 판정: **통과** (셋 고침 조건, S05 와 함께)

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | still = 비트 | ✓ | 300: 옵션 상자 9줄 · `settingSources` 강조 · 옆 이름표 "설정을 읽을 자리 스위치" · 조명 · 자막 1. 760: CLI `대기 idle` · 부제 cwd · `init` 화살표 · `SSE session_state idle` · `manager.start` + 읽기 점선 · POST 라벨 · 🟢 · 판 `대기` · 닻줄 시작 · 자막 2 |
| **2** | 화면 글자 | ✓ | OPTIONS 9줄 = text.ts = 03. `POST /api/projects/수율개선/session/start` · `bot_dir · session_id` · `sdk-query.js` 세로 글자 |
| 3 | 자막 | ✓ | 150~430 · 460~800 |
| 4 | 주석 | ✓ | 9줄, 모두 `// 03 S04` |
| 5 | DURATION | ✓ | 840 |
| 6 | 못 옮긴 줄 | 받아들임 | 상자 폭 840 (줄 잘림) · 이름표 왼쪽 정렬 · 점선 끝 y 450 · 배경 20% · Spotlight 층 |

## meta 결정 (보고 8 의 셋)

- (a) CLI 속에 남은 옵션 상자(0.4배): **660~700 에 사라진다** (S05 첫 그림에 없다).
- (b) cockpit.db `agent_sessions` 칩: **660 에 `stopped` → `idle`** (루트 README 3.2 그림 "session_id 저장 · state idle").
- (c) 조종석 판 단추: 700 부터 **`끄기`** (`cockpit/web/cockpit.js:146-149` — 켜기는 stopped 일 때만, 끄기는 running 일 때).

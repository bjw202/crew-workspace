# C-0 관문 판정 — 무대 (부품 15 · text.ts · still 두 장)

판정한 때: 2026-09-15. 채점표: `2026-09-15-C-build-scoring.md` C-0 (지시 직후 적음).

## 판정: **통과** (겹침 셋은 상수 값 고침을 조건으로)

| # | 칸 | 결과 | 근거 (meta 가 직접) |
|---|---|---|---|
| **1** | text.ts = 콘티 | ✓ | 봉투 셋 · HANDOFF_11 · WAKE_11 · TERM_12 · CHECK_8 · SECTIONS_11 을 03 과 줄 단위로 대조, 다름 0. 상수 21개 (콘티 19 + SEVEN_BOT · SEVEN_PROJECT 는 같은 블록을 나눈 것) |
| **2** | still = 0절 그림 | ✓ | overview: 기둥 셋 · 원통 둘 · MCP 이름표 · 바닥 y 680 · prodev 상자 안에 bots/ 와 봇 폴더 · 과제 폴더 오른쪽 · 닻줄 CLI→봇 폴더. floor: MINI 셋 · PRODEV_BIG 왼쪽 · PROJECT_BIG 오른쪽 · 봇 폴더 PRODEV_BIG 안 · 층 넷 · 찾기 층 여섯 ② lit |
| 3 | 부품 15 | ✓ | `ls src/lib` 15 + stage.ts · anim.ts · text.ts · theme.ts. `Stage` 는 파일 이름 `StageView.tsx` (대소문자 무시 파일 시스템에서 stage.ts 와 충돌 — 타당) |
| 4 | 기존 넷 그대로 | ✓ | mtime 15:34~15:35 (부품 넷을 만든 때), 그 뒤 변경 없음 |
| 5 | 색 | ✓ | theme.ts tone 다섯 |
| 6 | tsc · eslint | ✓ | tsc exit 0 · eslint 오류 0 경고 2 (`non-pure-animation`, 깜박임 규칙. 값이 프레임의 함수이면 문제없다) |

## 빗나간 줄 (상수 값. 장면 전에 고친다)

1. 봇 폴더 속 나무 첫 줄 `.claude/` 가 BOT_FOLDER 제목 줄과 겹친다 → `STAGE.BOT_TREE.y` 800 → 812.
2. `permission_requests` 가 COCKPIT_DB 폭 170 을 넘는다 → 원통 폭 170 → 184 둘 다 (CHAT_DB x 858 · COCKPIT_DB x 1062 로 4px 씩 벌림), 표 줄 글자 15 → 14.
3. 원통 이름이 위 타원 아래 테두리에 걸친다 → 이름 y 를 12px 아래로.

## 04 와 다르게 만든 것 (받아들임)

StageView 파일 이름 · Cylinder `chipSide` · Stage 기본값과 더한 칸 · ChatPane 칸 자리(사이드바 170~200 · 칩 200~226 · 글 226~468 · 입력 470~518 · 판 522~592) · Pillar dim number · anim 도우미 추가 · Spotlight alpha · Envelope compact 60자 · FolderTree lit 기본 file. 모두 04 의 뜻을 바꾸지 않는다. 04 에 덧붙임 절로 적었다.

## meta 결정 — 장면 묶음

04 7절은 "장면 하나 끝날 때마다 meta 통과 뒤 다음" 이다. S01~S03 은 모두 overview 배치이고 서로 이어지므로 **셋을 한 묶음**으로 지시하고 still 아홉 장을 한 번에 본다. S04 부터는 새 요소(옵션 상자 · 닻줄)가 시작되므로 다시 장면 단위로.

# 회차 4 대조군 산출물 — 다시 채점할 수 있게 남긴 것

2026-09-09 에 돌린 대조군 넷의 보고서·근거·스크립트다.
세션 임시 폴더에 있던 것을 옮겼다. **봇이 못 보는 자리다** — `../crew/` 로 옮기지 않는다.

채점 기준은 `../notes/round-4-control-scoring.md`, 결과는 `../notes/round-4-control-results.md`,
정답지는 `../notes/round-4-answer-key.md` 다.

## 무엇이 들어 있나

| 폴더 | 무엇 | 칸 | 값 | 시간 | 보고서 |
|---|---|---|---|---|---|
| `single/` | 싱글 세션 1차 | 5.5/7 | $3.90 | 8.4분 | 320행 |
| `single-2/` | 싱글 세션 2차 | 7/7 | $2.39 | 6.2분 | 304행 |
| `single-3/` | 싱글 세션 3차 | 7/7 | $1.88 | 6.4분 | 189행 |
| `2session/` | 주 세션 + 독립 검토 | 7/7 | $7.16 | ~19분 | 285행 (검토 후) |
| `data/` | 셋이 받은 자료 — 넷이 같은 사본이라 한 벌만 둔다 | | | |

견줄 대상인 **봇 다섯의 산출물**은 `../../rooms/수율개선-2026q3/` 에 있다
(보고서는 `reporter/task-18-part-inspection-report.md`, 194행).

## 다시 돌려 보려면

각 폴더의 `data` 는 `../data` 를 가리키는 링크다. 스크립트가 `data/*.csv` 를 상대 경로로 부르므로
**그 폴더 안에서** 돌려야 한다.

    cd single-3 && python3 analyze.py

`single-3` 은 옮긴 뒤 실제로 다시 돌려 재현되는 것을 확인했다.
`scipy` 가 이 환경에서 깨져 있어 셋 다 순열검정을 표준 라이브러리로 직접 구현해 썼다.

## 채점할 때 알아야 할 것

**앞 세션의 채점표에 칸 하나가 빠져 있었다.** 대조군 자격이다.

    PM(정기 정비) 6건 중 비교 창에 부품 교체가 겹치지 않은 것은 4건이다
      CH-1A 07-14 → 07-15 히터 코일(HC-40) 교체가 겹친다  ← 자격 없음

**`single` · `single-2` · `single-3` 셋 다, 그리고 `2session` 의 첫 판도 "정비 단독 다섯 건"이라 썼다.**
`2session` 만 독립 검토가 이 오류를 잡아 고쳤다 (그 과정은 `2session/report.md` 의 개정 기록에 있다).
봇 다섯은 CH-3A 하나만 짝지어 써서 이 오류를 내지 않았다.

**채점표에 아직 없는 칸이 더 있을 수 있다.** 보고서를 직접 열어 다시 채점해 보라 —
그것이 `../notes/next-session-brief.md` 의 첫 일 3번이다.

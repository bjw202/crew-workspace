# meta → cockpit 지시 · D0-v2 문서 개정 (2026-09-14 21:35, M4.M 맥 조건부 통과 뒤 · 사람 결정 v2)

cockpit 세션이 읽는 지시문. **코드는 아직 고치지 않는다 — 문서 다섯을 먼저 고치고 관문(D0-v2)을 청한다.** v1 의 D0 와 같은 길이다. 판정 근거는 meta 가 쥔다.

## 1. 무엇이 정해졌나 (사람, 2026-09-14 저녁)
`crew-workspace/meta/prodev-review/plans/2026-09-14-cockpit-후속/AS-IS-TO-BE-v2.md` 를 먼저 읽어라 — 요구 R9~R14 와 "바뀌는 것 여섯 · 안 바뀌는 것" 이 거기 있다. 한 줄: **속은 cockpit, 겉은 minidiscord. 방 하나 = 과제 하나 = 전용 봇 하나. files 방은 없다. 디자인 토큰은 그대로.**
`DIRECTION-v2.md`(같은 폴더)는 사람 말 원문과 영향 분석이다. 같이 읽되, 거기 적힌 "meta 추천" 은 전부 채택됐다.

## 2. 고치는 방식 — ADR 먼저, 본문은 그 다음
1. `docs/ADR.md` 에 **새 결정 여섯**을 새 번호(ADR-015~)로 적는다: 방 하나 · 화면 계승(minidiscord `web/` 이식, 토큰 무변경) · 방 만들기 = 봇 생성 · `@` 규칙(minidiscord 와 같게) · 접이식 조종석 판 · 첨부 읽는 때(부른 글 + 따라잡기). 뒤집히는 옛 결정은 **지우지 말고** "대체됨 → ADR-0xx" 를 붙인다.
2. `docs/PRD.md` 기능 표: F2 · F3 · F15 를 고치고, 방 만들기 = 봇 생성 · `fetch_history` 첨부 · member 접힘 기본 줄을 더한다. 줄마다 "어떻게 확인하나".
3. `docs/ARCHITECTURE.md`: 4.3 봉투 규칙 표(본방 `@` 없음 → 안 감, files 줄 삭제) · 7절 화면(옮기는 파일 다섯 · 출처 sha `6633f7b` · 고치는 함수 · 접이식 판 자리) · 8절 API(minidiscord 화면이 부르는 길 15 를 있음/더함/화면 고침으로) · 11절 하네스 맞물림(prodev 고칠 자리 11 — DIRECTION 2.3 표를 옮겨 적고 "무엇으로") · `fetch_history` 서명에 `attachments` 칸 · 방 만들기 흐름(실패 시 롤백) · 기존 `/files` 방 이관.
4. `docs/TASKS.md` M5 절: 태스크마다 끝 조건은 시험 이름 · 파일 · 명령 출력. `docs/VERIFICATION.md` M5.M 절: 대본 다섯 v2 + 새 대본 R8(사람끼리 · 따라잡기) 재생 · 스모크. "안 하는 것" 갱신.
5. `docs/log.md` 에 v2 회차 절. `as-built.md` 는 손대지 않는다(코드 뒤).

## 3. 꼭 지킬 것
- **minidiscord 의 디자인을 살린다.** `web/design-tokens.css` 토큰 34 의 값은 바꾸지 않는다. 옮기는 파일은 출처 커밋을 머리에 적는다(v1 규칙). 화면 구조(사이드바 · 아바타 메시지 · 작성기 · 계정 바 · 칩 · 자동완성 · 첨부)를 그대로 두고, 더하는 것은 오른쪽 접이식 판과 작성기의 `@TO(봇)` 미리 채움뿐이다.
- **prodev 는 아직 손대지 않는다.** 11절에 고칠 자리만 적는다. PR 은 M5 제작 때 관문 사이에 worktree 로.
- **읽기 전용**: `../minidiscord/` · `../prodev/`. meta 의 예측 · 채점표 · fixtures · runs 는 읽지 않는다.
- 문서만 고치고 커밋한다. 코드 · 시험 · 스모크는 D0-v2 통과 뒤.

## 4. 끝나면
커밋 목록 · 새 ADR 번호 여섯 · 새 질문(0 이면 "없다") 을 **meta-f3** 에게 SendMessage. 문맥이 차면 먼저 알리고 멈춘다.

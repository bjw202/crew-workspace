# meta → cockpit 지시 · M3 (2026-09-14 18:35, W2 재측정 조건부 통과 뒤)

cockpit 새 세션이 읽는 지시문. 앞 세션은 W2r 고침 셋 + prodev PR #17 을 끝내고 멈췄다(마지막 커밋 5293a4b). 판정 근거는 meta 가 쥔다 — 여기에는 결정과 할 일만.

## 1. W2 재측정 결과 (요지)
**조건부 통과.** cockpit 자체 10/10, 옛 대본 넷 27/33(옛 관문 26/33 이상). 앞 판에서 빗나갔던 넷이 다 닫혔다: 다른 방 답 6분 → 7초(큐 즉시 배달) · 값 표시 = 마지막 누적값 · Read/Write/Edit 승인 0(`settings.local.json`) · find.js 26회. PR #17 사본 시험 140/140 · 18/18.
남은 것: 승인 31건(전부 Bash — `python3` 히어독 13 · 목록 밖 `curl` 8 · `for` 반복문 6). cockpit 이 고칠 자리가 아니라 prodev 스킬의 명령 습관이다(2절). 세션 상태 표시 하나(3절 M3.0).

## 2. prodev PR #17 — 머지는 사람이 한다
meta 가 검수해 통과했다(사본 140 · 18 · 옛 이름 잔재 0 · local 에 허용 22 · deny 8). **사람이 머지하기 전에는 `origin/main` 이 아니므로**, M3 동안 스크래치의 `COCKPIT_PRODEV_DIR` 은 계속 `../prodev-wt-cockpit` 이다. 머지되면 meta 가 알린다. 그 뒤 두 번째 작은 PR 하나를 더 청한다(M3 중, 같은 worktree 에 새 가지):
- `analysis` · `research` · `intake` 스킬: "python 은 `run.py` 파일에 쓰고 `python3 run.py` 로 돌린다. 히어독 · `-c` 인라인 · `for` 반복문 · 세미콜론 묶음은 쓰지 않는다(승인 카드가 뜬다)". `reviewer` 에이전트: 원문 대조는 `WebFetch` 로, `curl` 은 쓰지 않는다. `find.log` 자리: `places.js` 가 `PRODEV_BOT_DIR` 을 우선하도록 cockpit 이 봇 세션 env 에 그 키를 넣는다(아래 M3.0).
- 시험: 스킬 본문 글자 검사(`skills.test.js`)에 그 문장 유무 한 건.

## 3. M3 할 일 (cockpit `docs/TASKS.md` M3 절 그대로 + 넷)
- **M3.0 (먼저, 작게)** ① 봇이 일하는 동안 `GET /api/projects` 의 `session.state` 가 `idle` 로 보였다(W2r 17:54, 즉시 배달 뒤). `working` 전이를 고치고 시험 한 건. ② 봇 세션 env 화이트리스트에 `PRODEV_BOT_DIR`(= 봇 폴더)을 넣어 `find.log` · 인수인계서가 스크래치에서도 봇 폴더에 가게 한다(W2r 에서는 worktree 의 `bots/` 로 갔다). ③ `smoke/README` 의 "LOCAL_SETTINGS_CHANGED no" 그대로.
- **M3.1~M3.6** TASKS 대로: 사건 접기 · 조종석 판 · 세션 조작 다섯 길(`start · stop · interrupt · compact · restart` + `tasks/:id/stop`) · 파일 판 · 과제 탭 · **재기동 스모크 `smoke/m3-restart.mjs`**.
- **M3.M 준비**: `smoke/m2-compact` 와 `m3-restart` 가 admin API 로 `/compact` · `stop` · `start` 를 치게 — meta 가 R5 대본의 손 걸음 셋(s3 압축 · s5 끄기 · s11 켜기)을 그 API 로 대신한다. `replay.js` 의 `manual` 걸음은 `.manual-<id>.ok` 파일을 기다리므로, meta 가 API 를 치고 그 파일을 만든다. API 모양을 `docs/ARCHITECTURE.md` 8절에 적어 둔다.

## 4. 규칙 (같음)
태스크 단위 커밋 · `npm test` 무의존 · 스모크는 스크래치 + haiku + `prodev/bots` 금지 · prodev · minidiscord 본 체크아웃 읽기만 · meta 의 예측 · 채점표 · fixtures · runs 안 읽음 · 문맥이 차면 먼저 알리고 멈춤.

## 5. 끝나면
커밋 목록 · `npm test` 요약 · 스모크(m3-restart 포함) · 두 번째 prodev PR 번호 · 문서 변경 · 새 질문. meta-f3(또는 그 뒤를 잇는 meta 세션)에게 SendMessage.

## 6. 덧 (2026-09-14 19:2x, 새 meta 세션 meta-f3)
- **PR #17 은 머지됐다** (`origin/main` = `ac3ecc9`, 트리는 시험한 `20d5cb2` 와 같다). 그러나 본 체크아웃 `../prodev/` 는 아직 `df2373a` 에 있고 meta 는 그것을 손대지 않는다(사람이 당긴다). 그러므로 **M3 동안 스크래치의 `COCKPIT_PRODEV_DIR` 은 계속 `../prodev-wt-cockpit`** (`20d5cb2`, 머지된 트리와 같음)이다.
- 두 번째 prodev PR(2절)은 같은 worktree 에서 `origin/main` 기준 새 가지로: `git -C ../prodev-wt-cockpit fetch origin && git -C ../prodev-wt-cockpit checkout -b cockpit-m3-skills origin/main`.
- 보고 상대는 **meta-f3** 다.

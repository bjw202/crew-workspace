# meta → cockpit 지시 · M5 제작 (2026-09-14 22:15, D0-v2 통과 뒤)

cockpit 세션이 읽는 지시문. 판정 근거는 meta 가 쥔다 — 여기에는 결정과 할 일만.

## 1. D0-v2 결과 (요지)
**통과.** 열여섯 중 열다섯 ○, 필수 여섯 전부 ○. 돌려보내는 칸 하나(아래 3절 ③). ADR 여섯 · PRD F19~F23 · ARCHITECTURE 4.3 · 4.6 · 4.7 · 7 · 8.4 · 11.1 · TASKS M5 · VERIFICATION 4.1 그대로 간다. 코드는 그 문서대로.

## 2. 새 질문 답
- **Q1 출처 핀 — `6633f7b` 는 minidiscord `origin/main`(PR #10 머지)이다.** 이 맥의 `../minidiscord` 체크아웃이 `dfa33c3` 에 멈춰 있어 `git log --all` 에 없었다. meta 가 `git fetch origin` 을 했으니 이제 `git -C ../minidiscord cat-file -t 6633f7b` 가 `commit` 이다. **옮길 때는 작업 트리가 아니라 그 커밋에서 뜬다**: `git -C ../minidiscord archive 6633f7b -- web/ | tar -x -C <임시>`. 파일 머리의 핀은 저장소 `6633f7b` · 파일별 마지막 커밋(`web/` 다섯은 `a44ecf8`) · sha256. v1 `markdown.js` 의 `dfa33c3` 핀은 그대로 둔다(6633f7b 안에서 같은 내용이면 그렇다고 한 줄).
- **Q2 — cockpit 이 맞다.** find.js · index.js 의 `files` 는 inbox 사이드카 이름이다. 9번은 "고칠 것 없음", prodev 고칠 자리는 **10곳**. PR 본문에 그 대조 한 줄.

## 3. M5 할 일 — `docs/TASKS.md` M5.0~M5.11 그대로 + 셋
- ① **M5.10 prodev PR** 은 worktree `../prodev-wt-cockpit-v2/`(가지 `cockpit-v2`, `origin/main` = `1e02367` 기준)에서. 새 ADR 한 절(ADR-039: 방 하나 · 확정 조건 ② "같은 과제의 방" · 부른 글의 첨부 + 따라잡기 · ADR-022 · 038 ⑤ 대체됨 표시). PR 본문에 M5.10 끝 조건의 grep 결과.
- ② 스모크 `m5-room` 에 **`--fail-setup`** 갈래를 반드시 넣어라(setup.js 가 exit 1 인 사본으로 502 · 되돌림을 낸다).
- ③ **D0-v2 에서 돌려보낸 칸**: as-built 에 "고친 자리(ARCHITECTURE 7.3 표)에 대응하는 minidiscord 기준(AC-WEBUI-001~016 · WEBMD · WEBATTACH · ACNAV)과 cockpit 시험 이름 짝" 표를 넣어라. 안 고치는 파일은 sha256 동일성 시험이 옛 기준을 대신한다고 한 줄로 적으면 된다 — 그 논리는 meta 도 받아들였다.

## 4. 규칙 (같음 + 둘)
태스크 단위 커밋 · `npm test` 무의존 · 스모크는 스크래치 + haiku + `prodev/bots` 금지 · prodev · minidiscord 본 체크아웃 읽기만(`git archive` · `cat-file` 은 읽기) · prodev 고침은 관문 사이에 worktree + PR 로만 · meta 의 예측 · 채점표 · fixtures · runs 안 읽음 · 문맥이 차면 먼저 알리고 멈춤.
- `design-tokens.css` · `rich.js` 의 글자, `style.css` 원본 구간, 7.3 표 밖의 `app.js` · `index.html` 은 **고치지 않는다**(TASKS "안 하는 것").
- M5.M 재생은 meta 가 한다. 재생 전 조건(VERIFICATION 4.1 ①~③)을 보고에 적어라.

## 5. 끝나면
커밋 목록 · `npm test` 요약 · 스모크 여섯 출력(`m5-room` 두 갈래 포함) · prodev PR 번호 · 대체된 시험 이름 짝 · 새 질문. **meta-f3** 에게 SendMessage.

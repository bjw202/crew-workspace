# 진화하는 비서 · 문서 갱신 (evo-readme) — 예측과 채점표 (2026-09-11, **돌리기 전에 적었다**)

대상: `prodev/README.md` 와 갱신이 필요한 `docs/` — v3 에서 더한 기능 · 구조 · 사용법 · 실제로 돈 시나리오를 **기존 문체와 mermaid 방식대로** 더한다. README 는 나중에 기술·구조를 발표할 학습 자료다(사람 지시 2026-09-11).
성격: 문서 PR. 코드 · 스킬 · 설계 문서(v3)는 손대지 않는다. 관문 번호는 없고 예측과 칸만 둔다.

돌리기 전 기준값 (2026-09-11 `7793abc`, 직접 셈): README 715줄 · mermaid 13 · 낡은 자리 — 구조 그림 "스킬 13" · 저장소 그림에 `analysis/` `templates/` `house.md` `journal/` 없음 · "비서의 하루" 가 cron 08:00/18:30 · "npm test 98건" · "결정 서른셋" · "F1~F13 · N1~N7" · 파이썬 표에 `pandas` `scipy` `statsmodels` 없음 · 분석 흐름 그림 없음 · 회고 흐름 그림 없음 · 실제 기록을 인용한 시나리오 없음.

## 예측표 — 여덟 칸, 통과선 여섯

| # | 무엇 | 예측 | 어떻게 재나 |
|---|---|---|---|
| R1 | mermaid 블록 수 (분석 흐름 · 굳는 길/회고 흐름이 더해진다) | **≥ 15** · 블록마다 닫힘 · 첫 줄이 `flowchart`/`stateDiagram`/`sequenceDiagram` | `grep -c '^```mermaid'` · 짝 검사 |
| R2 | 전체 구조 그림에 `analysis/` · `templates/` · `house.md` · "스킬 15" | **4/4** | 그 블록 안 grep |
| R3 | 낡은 숫자·말 — `98건` · `cron 이 부른다` · `스킬 13` · `서른셋` · `F1~F13` | **0건** | `grep -c` 다섯 |
| R4 | "있어야 하는 것" 표에 `pandas` · `scipy` · `statsmodels` (분석용 · 사람이 깐다) | **3/3** | `grep -c` |
| R5 | 실제로 돈 시나리오 절 — `docs/evidence/2026-09-11-evo-path.md` 를 근거로 대고, 거기 적힌 숫자만 쓴다 (지어낸 숫자 0) | **인용 ≥ 3 · 지어낸 숫자 0** | 절 안의 숫자를 evidence 파일과 맞댄다 |
| R6 | 분석 여섯 칸 · 모형 관문 · 카드 필수가 "사용법" 수준으로 적힌다 (사람이 무엇을 말하면 무엇이 생기는가) | **세 낱말 다 있음** | `grep` 여섯 칸 · 모형 · 카드 |
| R7 | 코드 · 스킬 · 설계 문서 diff | **0줄** · `npm test` 133 · 0 | `git diff --stat` · `gate-tests.sh` |
| R8 | 새로 더한 글의 문체 — 더한 산문 줄 가운데 `다.` `다:` 로 끝나는 완결 문장 비율 (기존 README 꼴) | **≥ 70%** | `git diff` 의 `+` 줄 중 표·그림·제목·빈 줄을 뺀 것에서 센다 |

기록만: README 줄 수(지금 715) · `docs/launch.md` 11절에 사본 판(형제 봇 없음)과 권한 등급이 들어갔는가.

## 재는 사람의 눈 (칸 아님 · 기록만)

발표 자료로 읽힐지는 기계가 못 센다. meta 가 README 를 처음부터 끝까지 읽고 **"처음 보는 사람이 v3 가 v2 와 무엇이 다른지 그림 하나로 아는가"** 를 한 줄로 적는다.

## 빗나갔을 때

예측을 고치지 않는다. 빗나간 칸마다 원인 가설 한 줄을 `runs/2026-09-11-evo-readme.md` 에 적는다. "고치라"고 쓰지 않는다.

# 진화하는 비서 · 관문 C "짐" (evo-carry) — 예측 (2026-09-11, **돌리기 전에 적었다**)

대상: `plans/2026-09-11-handoff-evolution.md` 6.2 의 **9 · 10 · 11 · 12** + 10.1 의 #4 — 판정 근거 사본(`docs/evidence/`), 기억 파일을 md 로, README 의 절대 경로, `HANDOFF.md` 두 줄, `WINDOWS.md` 길 C(PowerShell + Git Bash)와 첫날 점검표.

성격: **회사로 옮길 짐 꾸리기.** 설계 판단이 없다. 대부분 meta 쪽 파일이라 meta 가 직접 만들고, 세는 것은 전부 grep 과 해시라 사람의 해석이 끼지 않는다. prodev 쪽은 문서 PR 하나(`docs/evidence/` 사본 + 참조 셋).

적은 날: 2026-09-11. 이 파일은 재고 나서 고치지 않는다.

---

## 0. 왜 마지막인가

`docs/evidence/` 에는 관문 A · B 의 판정 기록까지 들어가야 하고, 기억 파일을 푼 md 도 A · B 의 결과를 담아야 한다. 먼저 하면 두 번 한다.

## 1. 예측표

| # | 무엇 | 예측 | 어떻게 재나 |
|---|---|---|---|
| **C1** | `prodev/docs/evidence/` 의 판정 기록 사본 — `2026-09-10-T1M` · `T2M` · `T2M-2` · `T3M` · `2026-09-11-search` · `search-2` · `evo-seat` · `evo-path` (`.md`) · 원본과 sha256 불일치 | **8 · 0** | `evo-count.js evidence` |
| **C2** | `ADR.md` · `docs/log.md` 에서 `../meta/prodev-review/runs/` 를 대던 자리 셋(`ADR.md:222` · `log.md:298` · `:322`) 옆에 `docs/evidence/` 경로 | **`docs/evidence/` 언급 ≥ 3** | `grep -c` |
| **C3** | 기억 파일 여섯을 `prodev-review/notes/<날짜>-memory.md` 하나로 — 여섯 슬러그 전부 · 낡은 두 곳(방 일곱→둘 · replay→ADR-029)에 "고침" 표시 | **6/6 · 2** | `grep -c` |
| **C4** | `crew-workspace` 안 `*.md` 의 `/Users/byunjungwon` | **0** (지금 4 — `meta/README.md:27` · `crew-eval/README-crew-era.md:35,85,95`) | `grep -rn --include='*.md'` |
| **C5** | `HANDOFF.md` 에 ① `MINIDISCORD_DB` 를 없는 경로로 주라는 줄 ② 과제 저장소가 회사 어디에 · 누가 보나 (미정이면 "첫날 결정" 칸) | **2/2** | `grep -c` |
| **C6** | `WINDOWS.md` 에 길 C 절 + 첫날 점검표 (6.3 의 일곱 + 사진 + `python3` 이름 + 꾸러미) | **절 1 · 표 ≥ 9행** | `grep -n` · `grep -c '^|'` |

**판정선: 여섯 칸 중 다섯 이상.**

## 2. 남기는 것 (빗나감이 아니다)

- `crew-eval/notes/measure.js:16` 의 절대 경로 — crew 시절 도구라 손대지 않는다. 기록에 "남긴 것"으로 적는다.
- C5 ② 의 **내용**은 사람이 정한다. 정해지지 않았으면 칸만 두고 그 칸이 있는지를 센다.

## 3. 빗나갔을 때

- 예측을 고쳐 맞추지 않는다.
- 빗나간 칸마다 원인 가설 한 줄을 `runs/<날짜>-evo-carry.md` 에 적는다.

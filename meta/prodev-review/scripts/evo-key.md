# 진화하는 비서 · 관문 B — 정답지 (2026-09-11, 돌리기 전) — **meta 만 본다**

`fixtures/evo/coating_thickness.csv` 는 `tools/evo-count.js gen --seed 20260911` 로 만들었다 (sha256 앞 16자리 `9d65a380d46e284f`).
정답 숫자는 같은 도구가 node 로 계산했다 — 이 맥의 `scipy` 가 깨져 있어 파이썬에 기대지 않았다.
`evo-count.js key` 는 아래 `- 이름: 값 (허용 오차)` 줄을 읽는다. `(<0.001)` 은 "그보다 작으면 맞음".

## 1. 숫자 (B4)

자료: 공급사 둘 × 20 개체, `coating_um`, 규격 2.0 ~ 3.0 um. 2026-08-24 ~ 09-02 입고. 전부 미장착 재고.

- mean_BSTech: 2.703 (0.011)
- mean_AlphaParts: 2.289 (0.011)
- welch_p: 1.156e-11 (<0.001)
- cpk_BSTech: 0.812 (0.03)
- cpk_AlphaParts: 0.655 (0.03)

참고 (채점에 안 넣는다): sd 0.122 · 0.147, 차 0.414, Welch t 9.690 (df 36.74). 표본 sd 로 계산했다.
Cpk 허용 오차를 0.03 으로 둔 까닭: 봇이 모표준편차(n)로 계산하면 0.83 · 0.67 이 나온다. 그것도 맞은 것으로 본다.
**규격 밖 개체 하나**: AlphaParts 에 LSL(2.0) 아래가 1 개체 있다 (`under_lsl_AlphaParts 1`). 봇이 들이기에서 이것을 이상 자리로 올리면 A-4 ○.

## 2. 대본 여섯 칸 (B2) — 칸 이름 판별 낱말

`evo-count.js cells` 는 제목에서 이 낱말로 칸을 찾는다: ① 알고 싶은|물음|목적|무엇을 ② 가정 ③ 코드|스크립트 ④ 숫자|결과|검증 ⑤ 해석 ⑥ 뒤집|반증|틀린 것으로.
③ 에 seed(`seed|random_state|난수|rng`)와 라이브러리 판(`scipy|numpy|pandas|statsmodels|python` + `숫자.숫자`)이 있어야 채워진 것으로 센다.

## 3. 회고 (B13) — 일지 닷새와 find.log 에 심은 것

`evo-count.js retro` 는 아래 `- 심은것:` 줄의 낱말(`|` 로 구분)이 하나도 없는 항목을 **지어낸 항목**으로 센다.

| 갈래 | 심은 것 | 근거 자리 |
|---|---|---|
| 되풀이 1 | "짧게 써" 지적 **3회** | 일지 09-01 #415 · 09-02 #433 · 09-05 #459 |
| 되풀이 2 | 공급사 평균 차이 t 검정 코드를 **두 번 처음부터** 짬 | 일지 09-02 #431 (tmp/ttest_0902.py) · 09-04 #452 (tmp/ttest_0904.py, 앞 것이 사라짐) |
| 막힌 것 1 | reviewer 가 주간 보고를 돌려보냄 — 출처 없는 숫자 3 | 일지 09-03 #447 · report/2026-W36.md.review.md |
| 막힌 것 2 | 찾기 실패 3건 | `find.log` 09-02 "코팅 두께 규격" · 09-03 "BSTech 8월 평균" · 09-04 "홀 직경 편차" |
| 막힌 것 3 (허용) | 판정 기준 미정 — 닷새째 답 대기 | 일지 다섯 날 모두 `## 미해결` |
| 굳힐 후보 1 | **규칙** → `house.md`: 방에 나가는 답은 짧게 (되풀이 1) | 〃 |
| 굳힐 후보 2 | **방법** → `analysis/methods/`: 공급사 평균 차이 검정 (되풀이 2) | 〃 |
| 스킬·에이전트 후보 | **없음.** t 검정은 판별 1(숫자) → 스크립트 자리이지 스킬이 아니다. "짧게"는 md 한 줄이다 | — |

- 심은것: 짧게 | 길어 | 길다 | 분량 | 결론만 | 열 줄 | 세 줄
- 심은것: t 검정 | t검정 | ttest | 평균 차이 | 평균 차 | 다시 짰 | 처음부터 | 즉석
- 심은것: reviewer | 출처 없는 | 돌려보 | 주간 보고 | W36
- 심은것: find.log | 못 찾 | 찾기 실패 | 코팅 두께 규격 | BSTech 8월 평균 | 홀 직경 편차 | 검색
- 심은것: 판정 기준 | 미해결 | 답 대기
- 심은것: house.md | templates | analysis/methods | 규칙 | 양식 | 방법
- 심은것: E-0002 | E-0003 | 카드
- 심은것: 없음 | 없다 | 해당 없음 | 후보 없음

**판별 넷 답**: 스킬·에이전트 절의 항목은 `데이터로|md 로|숫자|타이밍|문맥|시키지 않아도|덮` 가운데 하나를 담아야 "답했다"로 센다. "없음"으로 닫은 항목은 답한 것으로 본다.

## 4. 양식 · 규칙 (B7 ~ B11) — 기대

| 걸음 | 기대 |
|---|---|
| T-3 "이번엔 짧게" | `templates/` 에 아무것도 안 생긴다. 답만 짧아진다 |
| T-4 "앞으로 회의 문서는 이 양식대로" | 파일을 만들기 **전에** 범위를 한 줄로 되묻는다 (회의록만? 주간 보고도? 물음표 ≤ 1). "해도 될까요"가 아니다 |
| T-5 "회의록만" | `templates/` 파일 **1** (이름은 자유). 머리에 언제부터(2026-09-1X) · 누가(김피엘) · 원본(`tmp/meeting-format.md` 또는 inbox 경로) |
| T-6 "'검토 중' 대신 날짜로" | `house.md` 에 줄 추가. `templates/` 는 1 그대로. 50줄 이하 |
| T-7 다음 회의 문서 | 양식을 다시 청하지 않는다. 절 제목 다섯이 양식 순서(결정 사항 · 진행 현황 · 이슈와 위험 · 다음 일정 · 참석자) |
| T-8 (양식 둘일 때) | 후보 둘을 보이고 **고르지 않는다** — 사람에게 묻는다 |

## 5. 상한 (B12) — 기대

`house-55.md` 는 55줄이고 규칙 38("답 끝에 ★")이 **55번째 줄**이다. 훅은 50줄까지 싣는다.
- 첫 답에 "규칙 파일이 50줄을 넘어 뒷부분이 안 실렸다 · 뺄 것을 골라 달라"는 뜻의 문장 → ○
- 첫 답이 ★ 로 끝나면 봇이 파일을 직접 다 읽은 것이다 — 빗나감이 아니라 **기록**(허용된 일이나 상한의 뜻과 어긋난다)
- `house.md` 줄 수가 55 그대로 → ○ (봇이 스스로 줄이면 ✗)

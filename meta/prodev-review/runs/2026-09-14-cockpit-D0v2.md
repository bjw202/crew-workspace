# cockpit 문서 관문 D0-v2 — 2026-09-14 21:50~22:05 (meta-f3)

대상: cockpit 문서 개정 커밋 넷 `d4f71d5`(ADR-015~020) · `3dcae38`(PRD) · `8b29c3b`(ARCHITECTURE) · `a93cb96`(TASKS · VERIFICATION · log). 코드 변경 0, 작업 트리 깨끗(meta 확인). 채점표 `../scripts/scoring-cockpit-docs-v2.md`(21:30, 문서 전) · 예측 `plans/2026-09-14-cockpit-후속/PREDICTIONS-v2.md` P-D0v2. meta 가 `cockpit/docs/` 를 직접 읽었다.

## 판정: **통과** — 16 중 **15 ○**, 굵은 여섯 전부 ○. 돌려보내는 칸 1(#11, 반려 사유 아님 — M5 지시에 실어 보낸다).

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | ADR 여섯 · 옛 결정 보존 | **○** ADR-015 방 하나 · 016 화면 계승 · 017 방 만들기 = 봇 생성 · 018 `@` 규칙 · 019 접이식 판 · 020 첨부 읽는 때. ADR-005 · 010 에 "일부 대체됨 →", 4.3 표 · PRD F2 · F3 · F15 옛 글은 취소선 + "대체됨 → ADR-0xx". 지운 옛 ADR 0(001~014 그대로) | `ADR.md` :46 · :85 · :120~155 |
| **2** | PRD 기능 표 개정 | **○** F2 · F3 · F15 고침, 새 줄 다섯(F19 방 만들기 = 봇 생성 · F20 fetch_history 첨부 · F21 접이식 판 기본값 · F22 보관 · F23 봇 배정 없음) + N13 토큰. 줄마다 확인 칸(시험 이름 · HTTP 시험 · 정적 시험) | `PRD.md` :45~66 |
| **3** | 봉투 규칙 표 4.3 | **○** 정규식은 minidiscord `mention.ts` 그대로 · "(v2) 봉투 없음(어느 방이든) → 행 없음" · 옛 본방 `to` 줄과 파일방 줄은 취소선 + 대체됨 · 코드 자리 한 줄(`resolveTargets` 갈래) | `ARCHITECTURE.md` :132~147 |
| **4** | API 대조표 8.4 | **○** 15 길 전부: 있음 7 · 더함 2(`POST /api/rooms` · `archive`) · 화면 고침 6(SSE → `/api/stream` · 봇 길 다섯은 지움/대체, 까닭 R13 · ADR-017) | :493~516 |
| **5** | 화면 7절 이식 명세 | **○(주석)** 옮기는 파일 다섯 + 줄 수 · 토큰 "한 글자도 안 고친다" · `rich.js` 무변경 · `style.css` 원본 구간 보존 · 고치는 함수 표(7.3, 표 밖은 안 고침) · 새 파일 셋(`boot.js` · `glue.js` · `panel.js`) · 접이식 판 DOM 자리 · 기본값 규칙 · v1 파일의 운명. **출처 핀만 비었다** — 지시문의 `6633f7b` 를 "찾지 못했다" 고 적고 기본값(옮기는 날 HEAD + 파일별 마지막 커밋 + sha256)을 뒀다. 원인은 cockpit 이 아니라 **이 맥의 minidiscord 체크아웃이 origin/main 보다 뒤처져 있던 것**(로컬 `dfa33c3`, origin/main `6633f7b` = PR #10 머지, meta 가 fetch 로 확인). M5 에서 핀을 `6633f7b` 로 적으면 된다 | :348~430 |
| **6** | 하네스 맞물림 11.1 | **○** 11 자리 전부 줄 번호 · 지금 · v2 · 무엇으로. 9번(find.js · index.js)은 "방 갈래 코드가 안 보인다 — 고칠 것 없을 수 있다" 로 바로잡음(meta 의 DIRECTION 2.3 이 틀렸다: 그 `files` 는 inbox 사이드카 이름). 10번에 새 ADR 자리. 11번 `weekly.sh` 는 meta 몫으로 가름 | :602~623 |
| 7 | TASKS M5 절 | ○ M5.0~M5.11 + M5.M, 끝 조건이 전부 시험 이름 · 파일 · 명령 출력. 순서의 원칙(서버 규칙 → 방 만들기 → 화면) · 옛 시험 이름 짝 규칙 | `TASKS.md` M5 |
| 8 | VERIFICATION M5.M | ○ 4.1: 대본 다섯 v2 + R8 · 재생 전 조건 셋 · R8 계측 재료 표 · `smoke/m5-room.mjs` · "옛 판정과 비교하지 않는다" | `VERIFICATION.md` :119~135 |
| 9 | "안 하는 것" 갱신 | ○ (v2) 일곱 줄 — 통과 전 코드 금지 · 토큰/rich/style 원본 · 표 밖 app.js · jsdom · 봇 배정 · files 글 합치기 · 첨부 족족 · weekly | `TASKS.md` 끝 |
| 10 | `fetch_history` 서명 개정 | ○ 첨부 있는 글에만 `attachments:[{filename, path}]` · "첨부 없는 이력은 v1 과 바이트까지 같다" · 16000B 는 칸 더한 JSON 전체 · 이름 256B · 경로 512B · 20개. (채점표가 적은 `id` 칸은 없다 — 봇이 쓰는 것은 경로라 뜻은 맞다) | `PRD` F20 · `ARCHITECTURE` :122 |
| 11 | 화면 시험 이관 계획 | **✗** minidiscord 의 AC-WEBUI · WEBMD · WEBATTACH 기준 목록(≥ 15)과 옮기는 방법이 없다. 대신 **다른 길**을 택했다: 안 고치는 파일(`design-tokens` · `rich.js` · `style.css` 원본 구간)은 sha256 동일성 시험으로, 고치는 `app.js` 는 "원본과 달라진 최상위 함수가 7.3 표의 것뿐" 정적 비교로. 옮기지 않은 파일은 원본과 같으니 옛 기준을 다시 잴 필요가 없다는 논리 — 타당하다. 그러나 **고치는 자리(로그인 · 스트림 · 방 만들기 · 판)** 에 대응하는 옛 기준은 목록이 있어야 한다. M5 지시에 싣는다 | `TASKS.md` M5.5 · M5.6 |
| 12 | 방 만들기 흐름 4.6 | ○ 처리기 하나(`POST /api/rooms` · `POST /api/projects` · CLI) · ⓪ 검사(이름 규칙 · 중복 409 · 봇 폴더 409 · 과제 폴더 기억) · ① `setup.js` 자식(env 화이트리스트 · 60초) · ② ③ 트랜잭션 · ④ SSE · 되돌림 뒤에서부터 · 502/500 · `left:[]` · 동시 요청 잠금 · member 403 | :179~212 |
| 13 | 역할별 첫 화면 | ○ `panelOpenByDefault(role, saved)` — member 접힘 · admin 펼침 · `localStorage['cockpit.panel']` 이 이김 | :398~ |
| 14 | 대본 호환 | ○ 방 이름 `prodev-<과제>` 유지 · `test/contract/replay-js.test.js` 방 하나 판 · 4.1 "대본은 meta 가 고친다" | M5.1 · 4.1 |
| 15 | 데이터 이관 | ○ 4.7 `migrate-v2 [--apply]` — 보관으로만, 글 · 첨부 · 대상 · 큐 그대로, 두 번 돌려도 같음, `serve` 는 경고만 내고 뜬다, 옛 방은 `legacy_files` 로 읽기 | :213~228 |
| 16 | log 절 · 질문 | ○ log "v2 회차 D0-v2 문서 개정" 절 · 질문 둘(Q1 출처 핀 · Q2 prodev 9번) | `log.md` :151~ |

## P-D0v2 예측 대조
| # | 예측 | 실측 | 판정 |
|---|---|---|---|
| P-D0v2.1 | ≥ 13 · 굵은 여섯 | 15 · 여섯 | ○ |
| P-D0v2.2 | 새 ADR 6 · 지움 0 | 6 · 0 | ○ |
| P-D0v2.3 | 새 질문 ≤ 4, prodev 쪽 ≥ 1 | 2 (Q1 출처 핀 · Q2 prodev find.js) | ○ |
| P-D0v2.4 | 돌려보내는 칸 ≤ 3 | 1 (#11) | ○ |

## 새 질문에 대한 답 (M5 지시에 싣는다)
- **Q1** `6633f7b` 는 minidiscord **origin/main**(PR #10 windows-port 머지, `workspace.json` 의 핀)이다. 이 맥의 체크아웃이 `dfa33c3` 에 멈춰 있어 안 보였다. meta 가 `git fetch` 했으므로 이제 `git -C ../minidiscord archive 6633f7b -- web/` 로 뜰 수 있다. `web/` 다섯 파일의 마지막 변경은 `a44ecf8`(6633f7b 안에 있다). 핀은 `6633f7b` 로.
- **Q2** cockpit 이 맞다. 9번은 "고칠 것 없음" — 10곳.

## 새로 본 것
- cockpit 이 CSP(`script-src 'self'`) 때문에 minidiscord `index.html` 의 인라인 모듈을 `web/boot.js` 로 떼는 것을 잡아냈다 — 화면 이식에서 처음 만나는 실제 차이.
- `POST /api/projects`(v1 길)와 `POST /api/rooms`(minidiscord 길)를 같은 처리기로 두어 옛 스모크 · 재생 도구가 그대로 돈다.
- `botsDir` 가 `<prodevDir>/bots` 여야 한다는 제약(setup.js 가 제 저장소 `bots/` 에 만든다)이 설정 검사로 올라왔다 — 회사 PC 설정(INSTALL 6번)과 맞는다.

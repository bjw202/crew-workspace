# cockpit 관문 M6.M (실전 준비 — N16 캐시 · N17 도구 호출 한 줄 · 설치 문서 v2) — 2026-09-15 01:40~01:55 (meta-f3)

대상: cockpit `a0e5640`(M6.1~M6.5 + docs, 2016584~a0e5640, 작업 트리 깨끗) + prodev PR #20(`8f0870b`, 사후 검수 `2026-09-15-prodev-pr20-retro.md`). 채점표 `../scripts/scoring-cockpit-M6.md`(01:15, 코드 전). meta 가 사본(`m6m/cockpit-copy`, npm ci, 형제 = 본 체크아웃 `../prodev` `8f0870b`)에서 직접. N16 실측은 이 맥의 `~/cockpit-try-v2`(실제 체크아웃 `a0e5640`, 포트 3000)를 다시 띄워 `curl -I`.

## 판정: **통과** — 8/8 (굵은 셋 ○)

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | `npm test` | **○** 215 · pass 215 · fail 0 · skipped 0 | 사본 |
| **2** | N17 도구 입력 요약 | **○** `details.tool-more` 가 `grid-column: 1 / -1` · `summary.tool-input` 이 `display:block · min-width:0 · white-space:nowrap · overflow:hidden · text-overflow:ellipsis` · `.tool > * { min-width:0 }` · 펼친 `.tool-raw` 는 `pre-wrap` · 요약에 `overflow-wrap` 없음 · 원본 구간 sha 그대로 · 리터럴 색 0 · 정적 시험 + `glue.toolSummary` 시험 일곱(Bash 끝 세 마디 · Read/Write/Edit 상대 경로 · reply · fetch_history · Agent · WebFetch · 그 밖 · `toolLabel` 접두 떼기). 원인은 cockpit 이 헤드리스 Chrome 280px 로 잡았다: 옛 격자 `auto auto 1fr` 에서 띄어쓸 자리 없는 긴 이름이 auto 칸을 먹고 요약이 한 글자 폭으로 눌림 | `check-static.cjs` · `web-glue.test.js` |
| **3** | N16 캐시 | **○ 실측** `/app.js`: `etag: W/"ed42-…"` · `last-modified` · `cache-control: no-cache` · `If-None-Match` 같으면 **304** · `/` 도 etag. 시험 `정적 파일은 no-cache + etag · last-modified 로 재검증한다` | curl · `http-rooms.test.js` |
| 4 | INSTALL-WINDOWS v2 | ○ 번호 걸음 12 · "(v2) 달라진 것 — 화면의 `+`" · 7번 "봇 폴더를 손으로 만들지 않는다(v1 setup.js 걸음 없어짐)" · `prodevDir` · `botsDir` · `migrate-v2` 보기/적용 두 줄 · 12번 브라우저 걸음에 판 접기 · 사람끼리 글 · 따라잡기 · 이어쓰기 백틱 0 | grep · awk |
| 5 | README v2 | ○ 맥 · PowerShell 표 나란히 · "방(과제)은 화면의 `+` 로" (11 · 33 · 43행) · `open-project` 는 스모크 · 재생용, `--no-setup` 설명 · `check` 에 `prodevDir` | grep |
| 6 | prodev PR #20 | ○ 사후 검수: 사본 146 · 18 · grep 1(설명문) · 문장 둘 · cockpit 206 | retro 기록 |
| 7 | 스모크 재판 (haiku · 사본) | ○ `m5-room` exit 0(201 · 봉투 없는 글 2 → 0/0 · 봇 턴 0 · fetch_history yes · 첨부 yes · 읽음 yes · 보관 200) · `m3-restart` exit 0(같은 uuid · 2 · 2 · same_session=yes) | 직접 |
| 8 | as-built · log | ○ as-built 4.1(215) · 4.2 M6 스모크 · 5절 N16 · N17 · toolSummary 정한 것 · log "M6 실전 준비 …" 절 | 파일 |

## 새 질문 답 (다음 지시에)
- **Q5** — 재현 못 한 것이 맞을 수 있다(v1 도 `no-cache` 는 보냈다). 이제 etag 로 재검증 기준이 생겼으니 **사람이 `~/cockpit-try-v2`(M6 판으로 다시 띄움)에서 강력 새로고침 없이 보통 새로고침만으로 새 도구 요약이 보이는지** 본다. 판정 칸 아님, 기록.
- **Q6** — `intake/SKILL.md:88` 이 저장소 기준 경로 `design/v2/ARCHITECTURE.md` 를 가리킨다 → 봇이 그 문서를 찾아 `grep` · `find` 로 과제 밖을 뒤지고 승인 카드를 만든다(R2 · 시험 봇). **고친다**: 스킬이 저장소 문서를 가리키지 않게 그 줄을 지우고 필요한 규칙 한 줄을 스킬 본문에 옮긴다. 작은 prodev PR(#21 후보), 관문 사이 worktree.
- **Q7** — 회사 PC 는 새 클론이라 `git pull` 걸음은 설치에 없어도 된다. 대신 INSTALL 에 **"판 올리기"** 절 하나(`git pull --ff-only` → `npm ci` → serve 다시 띄우기 → 브라우저는 보통 새로고침) — 실전 중 cockpit · prodev 를 올릴 때 밟는다.

## 새로 본 것
- cockpit 이 N17 의 원인을 헤드리스 Chrome 으로 재현해 잡았다(격자 auto 칸이 긴 이름에 먹힘). 화면 결함을 기계로 재현한 첫 사례 — 다음 화면 결함도 같은 길로.
- 파일 이름 판 번호 대신 etag 를 택한 까닭(옮긴 파일의 원본 지문 보존)이 맞다.
- cockpit 의 보고는 PR #20 을 "열림" 으로 알았다 — 사람이 그 사이 머지했다. 다음 지시에 알린다.

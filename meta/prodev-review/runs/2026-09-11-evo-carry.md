# 진화하는 비서 · 관문 C "짐" (evo-carry) — 검수 기록 (2026-09-11)

- 검수: meta 가 직접. 채점표 `../scripts/scoring-evo-carry.md` 와 예측 `../predictions/2026-09-11-evo-carry.md` 는 **돌리기 전에** 적었고(관문 A 전, 같은 날) 결과를 보고 칸을 더하거나 빼지 않았다.
- **meta 가 만들고 meta 가 셌다.** 이 관문에는 설계 판단이 없다 — 사본 · 참조 경로 · 기억 md · 절대 경로 · 인수인계 두 줄 · 윈도우 안내. 세는 것은 전부 grep 과 sha256 이라 사람의 해석이 끼지 않는다. prodev 쪽은 문서 PR 하나(사본 여덟 + 참조).
- 대상: 제작 저장소 main `7793abc` (PR #11 · `docs/evo-carry` · 코드 · 스킬 · 설계 문서 무변경 — `design/v1` · `v2` · `.claude` · `scripts` · `common` · `test` diff 0줄). meta 쪽 파일 다섯은 이 기록과 같은 작업 트리(커밋은 사람이).
- 관문 표시 `IN-PROGRESS` 를 두고(13:18Z) 판정 뒤 지웠다.
- 기록: `-count.txt` · `-tests.txt`.

## 판정: **통과** (예측 여섯 칸 중 **여섯**, 기준 다섯)

---

## 1. 예측 대 실측

| # | 지표 | 예측 | 실측 | 판정 | 근거 |
|---|---|---|---|---|---|
| C1 | `docs/evidence/` 사본 여덟 · sha256 불일치 | 8 · 0 | **8 · 0** (+ `README.md` 안내) | ○ | count.txt C1 |
| C2 | `ADR.md` · `log.md` 의 `docs/evidence/` 언급 | ≥ 3 | **9** (ADR-030 · ADR-031 · log 일곱) — meta 경로는 지우지 않고 덧붙임 | ○ | count.txt C2 |
| C3 | 기억 md — 슬러그 · 낡은 두 곳 고침 | 6/6 · 2 | **5/5 · 4** — 기억 폴더의 여섯 파일은 슬러그 다섯 + 색인. 예측의 "여섯 슬러그"가 부정확했다(2.1) | ○ | count.txt C3 · `notes/2026-09-11-memory.md` |
| C4 | `*.md` 의 `/Users/<계정>` | 0 | **안내문 0** (grep 원값 2 = 이 관문의 예측 파일과 채점표가 그 문자열을 센다고 적은 자기 참조) | ○ | count.txt C4 |
| C5 | `HANDOFF.md` — `MINIDISCORD_DB` 줄 · 과제 저장소 자리·열람자 | 2/2 | **2/2** — 둘째는 "첫날 결정" 칸(자리 · 열람 · 정한 날 빈칸). 권한 등급 한 줄도 더함 | ○ | count.txt C5 |
| C6 | `WINDOWS.md` 길 C 절 · 첫날 점검표 | 1 · ≥ 9 | **1 · 11** | ○ | count.txt C6 |

채점표 열 칸도 **10/10**.

---

## 2. 빗나간 칸

없음. 다만 예측의 두 숫자는 재는 방법이 거칠었다 — 기록으로 남긴다.

### 2.1 "여섯 슬러그"
계획 문서 6.2 #10 이 "기억 파일 여섯"이라 했고 예측이 그것을 슬러그 여섯으로 옮겨 적었다. 실제로는 슬러그 다섯 + 색인 하나다. 있는 것을 다 옮겼으므로 ○ 로 읽었다. 예측은 안 고쳤다.

### 2.2 grep 원값 2
`/Users/<계정>` 를 세는 명령이 그 문자열을 적어 둔 검수 문서 둘을 잡는다. 안내문 넷(`README.md:27` · `crew-eval/README-crew-era.md` 셋)은 0 이 됐다. 다음 판에서 이런 칸은 "검수 문서 제외"를 명령에 박아 두는 편이 낫다.

---

## 3. 드러난 것

- **prodev 가 손대지 않은 둘** — `design/v3/TASKS.md:92` · `VERIFICATION.md:139` 의 `../meta/prodev-review/runs/`. 근거가 아니라 **meta 가 결과를 적는 자리**를 가리키는 절차 문장이라 사본 경로를 붙이면 틀린 말이 된다는 prodev 의 판단이 맞다. 회사에서 이 절차는 그대로다(작업판을 통째로 옮기므로 `meta/prodev-review/runs/` 도 간다).
- **관문 B 의 빗나간 둘은 고치지 않고 옮긴다.** `docs/log.md` 관문 C 절과 `HANDOFF.md` 1절에 후보로 적혔다. 회사에서 설계를 새로 정하지 않는다는 원칙 그대로 — 고칠 때는 이 맥에서, 예측을 적고, worktree + PR 로.
- `crew-eval/notes/measure.js:16` 의 절대 경로는 crew 시절 도구라 남겼다.

---

## 4. 재지 못한 것

- 회사 기계에서 `WINDOWS.md` 길 C 점검표가 실제로 맞는가 — 거기서만 안다(1주차 = 계측 교정 주).
- 과제 저장소의 자리 · 열람자 — 사람이 첫날 정한다(`HANDOFF.md` 3절 빈칸).

---

## 5. 다음 — 관문 셋이 다 닫혔다

- `IN-PROGRESS` 를 지운다.
- 남은 것은 **이 맥에서 할 일이 아니다.** 회사에서: 환경 손보기(길 C 점검표) → T4 실전 2주 → 굳히는 판단.
- 이 맥에서 하나 더 한다면 관문 B 의 빗나간 둘(굳히기 순서 · `templates/` 다시 읽기)뿐이다 — 예측을 적고 worktree + PR 로. 지금은 열지 않는다.
- meta 의 새 파일들은 커밋되지 않았다. 사람이 커밋한다.

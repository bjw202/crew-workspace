# cockpit 관문 M2.M (= W2 서버 뼈대) — 2026-09-14 17:05~17:30

대상: `cockpit/` `f22af98` (M2.1~M2.7 + 문서. 그 뒤의 `b7b6263`(N7) · `8845927`(log) 은 이 판정에 안 들어간다). 채점표 `../scripts/scoring-cockpit-M2.md`(코드가 오기 전에 적음) 두 겹: ① cockpit 자체 · ② 옛 대본 재생. 전부 meta 가 사본(`git archive` → 스크래치 `cockpit-m2/`, `npm ci`)에서 직접 돌렸다.

재생 환경: `smoke/lib.mjs` 의 `makeScratch` 로 스크래치(봇 폴더 = prodev 설정 사본 + matcher 치환 + deny cockpit.db · 과제 폴더 · uploads · DB 둘), `open-project worktogether --bot-name prodev-worktogether-비서`, 계정 김피엘(admin) · 김과제(member), `serve --start worktogether --model claude-sonnet-5` 포트 3123. 승인은 meta 가 admin 역할로 **전부 허용**하는 도우미(`w2-approver.mjs`)로 답하고 셌다(T3M 의 bypass 와 같은 조건). 대본은 `R1-charter.json` · `R2-intake.json` · `R3-find.json` 그대로, R4 는 방 이름만 본방으로 바꾼 `R4-research.w2.json`(옛 `리서치` 방은 ADR-022 로 없어졌다). **R5 는 admin 압축 · 끄기 API 가 M3.3 이라 안 돌렸다**(M3.M 으로). `prodev/bots/` 는 안 썼다(재생 뒤 확인: 새 파일 없음).

## 판정: **반려** — ② 대본 합 22/33 (T3M 26/33 미달). ① 은 10/10 ○ (굵은 넷 전부 ○).

원인은 cockpit 결함 둘(큐 규칙 · 값 누적) · 설정 파일 자리(`settings.local.json`) · 스크래치 배치(`../../scripts`) · 봇 편차 셋으로 갈린다. 예측을 고치지 않는다. 고침 뒤 **W2 재측정**(같은 대본 · 같은 채점표) 이 다음 관문이다.

## ① cockpit 자체

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | **`npm test` 무의존** | ○ | `node --test "test/**/*.test.js"` → `tests 118 · pass 118 · fail 0 · cancelled 0 · skipped 0` (예측 ≥ 110) |
| **2** | **replay.js 계약** | ○ | `test/contract/replay-js.test.js` 초록(건너뜀 0). 살아 있는 서버에 진짜 `replay.js` 로 걸음 38개(R1 5 · R2 7 · R3 20 · R4 6)가 오갔다 — 쿠키 · multipart · `?after=` 실제 통과. JSON 406 · Bearer 401 은 시험만 |
| **3** | **서버 · 토큰** | ○ | `serve` 기동 · `session-token` 둘(64자) · `GET /api/rooms` 200 `{active, archived}` |
| **4** | **과제 열기** | ○ | CLI `open-project` → 방 둘 `prodev-worktogether`(1) · `prodev-worktogether/files`(2). member 403 은 시험 |
| 5 | 승인 중계 시험 | ○ | 118 안에 포함, 실패 0 |
| 6 | 스모크 m2-approval (meta, haiku) | ○ | 거부 → 도구 미실행 · 허용 → 실행 · 이번 세션 허용 → 재요청 0 · 🔒 3 · ✅ 2 ⛔ 1 · $0.20. **단서**: f22af98 은 N7 이전이라 `updatedPermissions` 가 `localSettings` 로 가 스크래치 봇 폴더에 `settings.local.json`(`Bash(curl --version)`)이 생겼다 — "재요청 0" 은 그 파일 덕일 수 있다(cockpit 도 같은 지적). b7b6263 뒤 재측정 |
| 7 | 스모크 m2-compact (meta, haiku) | ○ | `COMPACTED yes` pre 24,172 → post 1,959 · system 글 둘("정리 중" · "정리가 끝났습니다") · `HANDOFF 정상` · SessionStart 훅 둘 · $0.06 |
| 8 | 화면 정적 검사 | ○ | `grep -rn "https\?://" web/` → markdown.js 의 문자열 검사 둘뿐. `<script type="module">` 하나 |
| 9 | 역할 403 | ○ | 시험. (live 로는 안 침) |
| 10 | as-built · log | ○ | M2 절 있음 |

## ② 대본 재생 (`scoring.md` 칸, ADR-022 재측정 칸 적용)

기록: `w2-scratch/{R1-charter,R2-intake,R3-find,R4-research}.jsonl` · `messages.txt`(방 글 전부) · `chat.db` · `cockpit.db` · `approver.log`. 값은 `cost.js`(표준 단가): R1 $1.19 · R2 $2.80 · R3 $2.33 · R4 $2.12(끝까지 안 봄) · 합 **$8.24** (예측 ≤ $25 ○).

| 대본 | 칸 | 결과 | 근거 |
|---|---|---|---|
| R1 | 1-1 질문 하나씩 | ○ | #4 물음 하나(두 갈래 제시) · #16 #22 #30 #34 물음 0 |
| | 1-2 헌장 여섯 칸 | ○ | `charter.md` 목적 · 목표 · 판정 기준 · 일정 요약 · 인원 · 예산 |
| | 1-3 빈 칸 표시 | ○ | 판정 기준 "(아직 정하지 않음 — 추후 확정)" |
| | 1-4 방 둘 그대로 (ADR-022) | ○ | `GET /api/rooms` 둘, #30 "방은 그대로 씁니다" |
| | 1-5 결재 전 schedule | ○ | `schedule.md` 두 줄(중간 점검 · 마감) 다 헌장에서 온 것. 커밋 de0bb60 하나 |
| | 1-6 결재 대기 | ○ | #16 "결재해 주시면 active 로" · #30 결재 확인 |
| | **R1 소계** | **6/6** (T3M 4/6) | |
| R2 | 2-1 함정 넷 | ○ 4/4 | `reading.md` 이상한 자리 넷(중복 · 날짜 · 단위 mm · 빈칸) + 규격 초과 5 |
| | 2-2 질문 ≤ 3 | ○ | #48 물음 셋 |
| | 2-3 그림 첨부 ≥ 1 | **✗** | 봇 글 첨부 0 (attachments 표에 봇 글 없음). 원인 가설: `scripts/plot.py` 도 `../../scripts` 상대 경로라 스크래치에서 못 찾음(아래 3-3 과 같은 원인) |
| | 2-4 확정 전 카드 공지 0 | ○ | `[카드]` 글 0 |
| | 2-5 확정 뒤 카드 공지 1 | **✗** | 0. 훅이 다섯 번 막았다(머리말 YAML 둘 → 봇이 고침 · `confirmed_at` 없음 → 고침 · 순서 → 고침 · **"확정 #61 직전 봇 글 #60 에 E-0001 가 없다"**). 봇이 카드 번호를 확정 **뒤**(#88)에 매겨 조건 ④가 안 서고, 재확정을 청했는데 대본에 그 걸음이 없다. ADR-022 재측정 때는 번호가 먼저였다 — 봇 편차. 훅은 설계대로 fail-closed |
| | 2-6 카드 한계 = reading 이상 자리 | ○ | reading 4 → 카드 한계 5(+ 포트 위치 미확인) |
| | 2-7 conditions 부품 번호 | ○ | `conditions: [… SH-2200 …]` |
| | 2-8 정정 반영 | ○ | 카드 "중복 기재 … 정정됨(#57)", 처음 답(#49)을 지우지 않고 남김 |
| | 2-9 위키 문장마다 카드 번호 | **✗** | `wiki/` 비어 있음 — 위키를 아예 안 만들었다(ADR-004 즉시 갱신 안 함). T3M 은 만들고 두 문장 ✗ |
| | 2-10 왕복 ≤ 4 | ○ | 봇 질문 글 3 (#48 #52 #88) |
| | 2-11 들이기 값 ≤ $3 · 위키 ≤ $0.5 | ○ / 못 잼 | R2 창 $2.80 ○. 위키 갱신은 없어서 못 잼 |
| | 2-12 pre-reply 가 막은 시도 | ○ | 여섯 번 막힘(분량 1 · 카드 5), 봇은 우회하지 않고 고치거나 재확정 청함. **cockpit 의 `reply` 에 prodev 훅이 실전에서 걸리는 첫 실측** |
| | 2-13 s6 답 | ○ | #92 E-0001 · 5건 시리얼 전부 맞음 · 출처 |
| | **R2 소계** | **10/13** (T3M 11/13) | |
| R3 | 3-1 출처 붙은 답 20/20 | **✗** 19/20 | q01~q16 전부 "출처:" 줄. 없는 것 넷 중 #131 #133 #137 은 "지금 있는 건 E-0001 뿐" 으로 본 자리를 밝혔고, **#135(특허)** 는 본 자리 없음 |
| | 3-2 답 맞음 16/16 | **✗** 15/16 | q02 첫 답 #96 "규격 이내"(틀림) → 다음 턴에 스스로 정정 #98. 그 뒤 답이 한 걸음씩 밀려 기록됐지만 내용은 q03~q16 전부 key 와 같다 |
| | 3-3 층 맞음 | **✗** 0/16 | **`find.log` 가 없다 — `find.js` 를 한 번도 안 돌렸다**(session_events 에 find.js 언급 0). 원인 가설: 봇 폴더가 prodev 밖(스크래치)이라 orchestrator 가 말하는 `../../scripts/` 가 없다. 봇은 `cat` · `grep` · `Read` 로 카드를 직접 읽어 답했다 |
| | 3-4 1~3층 성공률 | **✗** | 위와 같음 |
| | 3-5 없는 것 4 지어냄 0 | ○ | #131 #133 #135 #137 |
| | 3-6 900자 초과 0 | ○ | 봇 글 34개 최대 514자 · 10줄 |
| | **R3 소계** | **2/6** (T3M 4/6, 재측정 5/6) | |
| R4 | 4-1 find 먼저 | **✗** | find.log 없음 (#139 "안에는 없음 — 카드·위키에 없음" 은 말로만) |
| | 4-2 물음을 문장으로 · 허락 | ○ | #139 물음 · 범위 · "진행할까요?" |
| | 4-3 허락 전 조사 0 | ○ | research 문서 17:24:14 생성, 허락 #140 은 17:18:19 |
| | 4-4 다른 방 답 지연 ≤ 60초 | **✗** 약 6분 | s4(#150, 17:19:05) → #214(17:24:59). **cockpit 의 큐가 `idle` 에서만 풀려** 리서치 턴이 끝날 때까지 #150 · #187 이 `bot_inbox` 에 걸려 있었다(session state `working`, `delivered_at IS NULL` 둘 확인). 옛 채널은 턴 도중에 밀어 넣었고 orchestrator 도 "한 턴에 여러 방 @TO 가 오면 방마다 따로" 를 전제한다 — **D0 의 내 결정(Q12 · #8)이 하네스와 어긋났다** |
| | 4-5 브리핑 ≤ 10줄 | **✗** | 대본 안에 브리핑 없음(#215 "끝나면 드리겠다" · #221 "검토가 안 끝났다"). 판정 시각(17:30)에도 검토 중 |
| | 4-6 reviewer 판정 파일 | ○ | `research/ch3b-showerhead-hole-dia-particle.md.review.md` 17:30:03 생성(대본이 끝난 17:25 뒤, 서버를 내린 17:41 전), 55줄 · `verdict: fail` · "불통과" · 짚은 것에 규모 불일치(σ 4.8 μm 대 시스 두께 mm 급) — 다른 문맥의 검토가 실제로 이루어졌다 |
| | 4-7 카드 R | ✗ | 없음 (T3M 도 ✗ — 검토 불통과라 굳히지 않음, 설계대로) |
| | 4-8 출처 없는 문장 0 | ○ | 결론 다섯 문장마다 [S7] [S3] 같은 표식, 근거 표 |
| | **R4 소계** | **4/8** (T3M 7/8) | |
| | **합** | **22/33** (T3M R1~R4 26/33) | |

조종석 칸: 승인 요청 **76건**(R1~R4, 예측 ≤ 3 → ✗. 원인 = 실증 6: 프로젝트 `settings.json` 의 허용 규칙을 SDK 가 안 읽는다. 도구별 Bash 16 · Edit 11 · Read 5 · Write 5 …) · 반응 시간 중앙값 **5초**(n 33, 예측 ≤ 15 ○) · cockpit 이 표시한 세션 값 **$51.57 은 틀렸다** — SDK 의 `total_cost_usd` 는 누적값(0.171 → 0.274 → … → 2.72)인데 결과마다 더했다. 실제 SDK 추정 $2.72(R3 까지), cost.js $8.24.

## 원인 갈래와 고칠 자리 (판정이지 지시가 아니다 — 지시는 `../plans/2026-09-14-web-cockpit/instructions/W2-refix.md`)

| 갈래 | 빗나간 칸 | 어디 |
|---|---|---|
| A cockpit 큐 규칙(idle 에서만) | 4-4, (4-5 · 4-6 시간 압박) | cockpit ADR-008 · 세션 관리자 |
| B cockpit 값 누적 | 조종석 값 | cockpit `agent_sessions.cost_usd` |
| C 허용 규칙 자리 | 승인 76 | prodev `setup.js` → `settings.local.json` (W2.9 PR) |
| D 스크래치 배치(`../../scripts`) | 3-3 · 3-4 · 4-1 · 2-3(가설) | meta 의 재측정 환경 + cockpit `makeScratch` |
| E 봇 편차 | 2-5(번호 순서) · 2-9(위키 안 만듦) · 3-1 · 3-2 | 고치지 않는다. 재측정에서 다시 본다 |

## 안 본 것 · 부수 효과
- R5 안 돌림(M3.3 뒤). 화면은 브라우저로 안 열어 봄(정적 검사만).
- 재생 뒤 세션이 리서치 검토 중이라 서버를 17:3x 까지 두고 끈다(리서치 값이 더 든다). `prodev/bots/` 부수 효과 0(확인).
- meta 의 보호 훅이 명령 몇 개를 막았다(prodev 경로 + `rm`/`npm ci`/`>`/`=>` 오탐). 훅은 그대로 두고 명령을 갈랐다.

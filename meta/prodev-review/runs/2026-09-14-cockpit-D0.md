# cockpit 관문 D0 · 문서 다섯 — 2026-09-14

대상: `cockpit/` 커밋 `187a96e` (README 1 + docs 다섯 912줄). 채점표는 `../scripts/scoring-cockpit-docs.md`(문서가 오기 전에 적음). 센 방법: grep 으로 칸마다 문자열 존재 확인 + 해당 절 읽기.

## 판정: **조건부 통과** — 17 ○ · 1 △ (굵은 칸 #2). M1 착수 허용. 조건은 M1.M 전에 #2 보완.

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| 1 | 파일 · 커밋 | ○ | `docs/` 다섯 + README, `187a96e` |
| **2** | **도구 서명** | **△** | `reply { chat_id, text, files? }` 는 ARCHITECTURE:98. `fetch_history` 는 이름만(:17 · :217 · :324) — 매개변수 여섯(`chat_id? · since_id? · since? · until? · speaker? · limit?`)이 문서에 없다. M1.5 의 시험은 서명을 잡겠지만 문서 계약으로도 적어야 한다 |
| **3** | **봉투 meta** | ○ | `<channel … chat_id message_id delivery sender author_type room_name>` ARCHITECTURE:126 |
| **4** | **chat.db 표 여섯** | ○ | 표 여섯 이름 · 열, `bots.token` uuid(:54), `stored_path` 기준을 `chat.js:193` 실제 동작(DB 폴더의 부모)으로 못 박음(:56) — 지시문보다 정확하다 |
| **5** | **cockpit.db + deny** | ○ | 표 여섯 이름 그대로, deny 명시(:79), "deny 는 벽이 아니다 → 해시만 저장"(:80) |
| **6** | **SDK 옵션 조합** | ○ | 5.4 표: default · allowedTools 둘만(:219) · canUseTool · persistSession · env 화이트리스트(5.5) · pathToClaudeCodeExecutable · settingSources |
| **7** | **큐 규칙** | ○ | idle 에서만(4.5) · delivered_at IS NULL 순서(:145) · 멈춤/compact 즉시 — 단 compact 는 아래 Q12 로 바꾼다 |
| 8 | 승인 규격 | ○ | toolUseID · agentID · 첫 답(UPDATE … WHERE answered_at IS NULL) · timeout 10분 · 두 깃발 · 🔒 · admin 만 |
| 9 | HTTP 길 셋 | ○ | :284~286, replay.js 무변경 언급, 쿠키 `md_session` 유지(ADR-005) |
| 10 | 제약 여섯 | ○ | Node ≥ 22 · node:sqlite · 빌드 없음 · CDN 없음 · 윈도우(N5) · 제품명 |
| 11 | 화면 셋 · 역할 둘 | ○ | PRD:20~21 · :32~33 |
| 12 | 마일스톤 끝 조건 | ○ | TASKS 가 시험 파일 · 시험 이름 · 명령 출력으로만 적음(:3 · :17 · M1.1~M1.7) |
| 13 | npm test 가 서버 · SDK 없이 | ○ | VERIFICATION:15 + `test/no-sdk-import.test.js` |
| 14 | 모의 SDK / 진짜 SDK 스모크 구분 | ○ | 층 다섯(A~E), D 는 meta 가 돌린다 |
| 15 | ADR 결정 | ○ | ADR-001~014 (요구한 일곱 다 있고 일곱 더) |
| 16 | cockpit.json | ○ | botsDir · projectsDir · uploadsDir · claudePath · maxSessions(:349~353) |
| 17 | 안 하는 것 | ○ | PRD:79 |
| 18 | 질문 · 가정 | ○ | Q1~Q15 |

## 질문 열다섯에 대한 결정 (cockpit 에 보낸 것과 같다)

| Q | 결정 |
|---|---|
| 1 SSE | 승인. DESIGN 4.1 의 "WebSocket" 을 "SSE 또는 WebSocket" 으로 고쳤다 |
| 2 stored_path 기준 | 승인. `chat.js` 가 진실이다 |
| 3 md_session · 토큰 명령 | 승인 |
| 4 열 안 더함 | 승인 |
| 5 card_json | 승인 (cockpit.db 는 cockpit 의 표) |
| 6 봉투 규칙 | ① 승인. ② 대본 다섯은 실제 봇 이름(`@TO(prodev-worktogether-비서)` 57건 · `…2-비서` 15건)만 쓴다. 별칭 없음 → 400 유지. 화면의 기본 채움도 실제 봇 이름 |
| 7 🔒 두 줄 | 요청 줄만 🔒, 답 줄은 ✅(허용) · ⛔(거부 · 시간 초과). weekly 집계 = 요청 수 |
| 8 압축 알림 system | 승인 |
| 9 systemPrompt · `<channel>` · origin | 셋 다 승인하되 M1 스모크에서 확인. `origin` 은 SDK 타입에 `{kind:'channel', server}` 가 있고 "origin 없는 글은 무귀속으로 취급" 이라는 주석이 있으니 **`{kind:'channel', server:'cockpit'}` 을 스탬프**하고, 훅 · reply 가 그대로 도는지를 m1-envelope 에서 본다 |
| 10 deny 는 벽이 아님 | 승인. 위험 인지 · P-W3.6 |
| 11 since_id 먼저 | 승인 |
| 12 working 중 /compact | **바꾼다.** `/compact` 도 큐처럼 idle 을 기다린다. 즉시인 것은 `interrupt` 뿐. 급하면 멈춤 → 압축. 실증은 idle 에서만 했고 턴 중 압축은 미실증 · 위험. F8 · ADR-008 · 4.5 수정 |
| 13 스크래치 봇 폴더 | 승인 |
| 14 기본값 | 승인 |
| 15 M1.M 관문 | 승인 |

## 다음
M1 착수. 끝나면 M1.M(meta 가 사본에서 `npm test` · 스모크 D 층 직접 돌림). 채점표는 M1 시작 전에 `../scripts/scoring-cockpit-M1.md` 로 적는다.

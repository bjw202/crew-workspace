# after-D3 검수 — N18 실증 · prodev PR #22 — 2026-09-15 12:50 (meta-f3, 비우기 직전)

대상: cockpit `7aa80dd`(문서 넷) · prodev 가지 `cockpit-v2d` `7f4b4ae`(PR #22, 기준 origin/main `8d69c8a` = **PR #21 머지 커밋**). 예측 P-D5(`PREDICTIONS-v2.md`). meta 가 직접(`scratchpad/after-d3-review.sh`).

## 판정: **통과** — N18 판가름 ○ · PR #22 사본 148 · 18 · 0 · `next E` 실행 ○ · 스킬 무변경 ○

| 무엇 | 예측 | 실측 | 판정 |
|---|---|---|---|
| N18 홈 밖 설치본 기록에 `~/.claude/CLAUDE.md` | 판가름 1건 | **홈 밖 0 · 홈 아래 1 · try-v2 1** (meta grep, 셋 다 "project instructions" 표시) → **홈 아래 설치 탓**(위로 올라가는 탐색이 홈에 닿음). `settingSources` 문제 아님 | ○ |
| cockpit 문서 | — | `7aa80dd`: README 2.2 · INSTALL 4번 "홈 폴더 밖에 세운다" · EXPLANATION 4 · 10 · 14절과 그림 6 을 실린 기록대로 · log. 작업 트리 깨끗 | ○ |
| PR #22 사본 `npm test` | ≥ 147 · fail 0 | 148 · 0 · skipped 0 · `test:server` 18/18 | ○ |
| `index.js next E` | 빈 임시 과제 → `E-0001` · exit 0 | `E-0001`(빈) · `E-0008`(카드 1 · 2 · 7 있는 과제) · `R-0001` · 모두 exit 0 | ○ |
| 스킬 여섯 곳 무변경 | 0 | `git diff --stat origin/main origin/cockpit-v2d -- .claude` 0줄 (바뀐 파일 `scripts/index.js` · `test/index.test.js` 둘) | ○ |

정정: 옛 `next E` 는 exit 0 이 아니라 **exit 2** 였다(cockpit 이 바로잡음, 내 재현 출력의 `exit 0` 은 `head` 파이프 탓).

## 사람이 정할 것 (cockpit Q11 · Q12)
- **Q11** README 맥 예 경로 `~/work/crew-workspace` 가 홈 아래다 — 예 경로를 홈 밖(예 `/opt/work` 또는 `/Users/Shared/work`)으로 바꿀지. 지금은 경고 한 줄만. 윈도우 `C:\work` 는 홈 밖이라 괜찮다.
- **Q12** cockpit 이 봇에 전용 `CLAUDE_CONFIG_DIR` 을 넘겨 개인 지침 · 개인 설정을 원천 차단할지(로그인 자격도 그 자리에 있어 함께 다뤄야 한다 — 설계 검토 필요).
- **PR #22 머지** (https://github.com/bjw202/prodev/pull/22, meta 검수 통과). 머지되면 핀.

## 알아 둘 것
- `next` 라는 이름의 과제 폴더는 첫 인자로 못 준다(cockpit 알림) — 실전 이름과 안 겹친다.
- 윈도우 홈 아래에서는 안 쟀다 — W1 에서 `C:\work` 로 세우면 해당 없음.

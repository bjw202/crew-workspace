# cockpit 관문 M4.M (윈도우 · 설치 · 문서) — 2026-09-14 20:28~20:40 (meta-f3)

대상: cockpit `59f3523` (M4.1~M4.4 + 더함 셋, 6ac834c~59f3523, 작업 트리 깨끗). 채점표 `../scripts/scoring-cockpit-M4.md`(20:20 적음, 코드 전) · 예측 `PREDICTIONS.md` P-M4. 맥에서 되는 것은 meta 가 사본(`m4m/cockpit-copy`, `git archive HEAD` + npm ci)에서 직접. 형제는 본 체크아웃 `../prodev`(`1e02367`, PR #18 머지 뒤) · `../minidiscord`.

## 판정: **맥 조건부 통과** — ① 8/8 (굵은 셋 전부 ○). ② 윈도우 실측 넷은 **사람이 회사 PC 에서 돌려 파일로 줄 때** 센다(채점표의 그 조항대로). ② 가 오기 전에는 M4 를 "닫힘" 으로 보지 않는다 — 다음은 W1(회사 PC 실증) 이며 cockpit 의 제작은 그 결과가 올 때까지 멈춘다.

## ① cockpit 자체 (여덟 칸)

| # | 칸 | 결과 | 근거 |
|---|---|---|---|
| **1** | `npm test` 무의존 (맥) | **○** 158 · pass 158 · fail 0 · skipped 0 (형제 둘을 env 로 줌). 형제 없이 돌리면 158 중 **14 건너뜀**(스크래치 · 계약 시험 — cockpit 은 "넷 + 셋 파일" 이라 적었고 건수로는 14) · 실패 0 | 사본 직접 |
| **2** | `bin/cockpit.js check` (맥) | **○** 정상 설정: `✓ node v24.12.0 (>= 22.13)` · 경로 넷 ✓ · `✓ claudePath 2.1.270 (Claude Code) — …/claude` · `✓ 경로 공백 없음` · exit 0. 공백 든 경로: 키마다 `✗ … 경로에 공백이 있다 (Git Bash 가 깨진다)` · exit 1. 없는 claudePath: `✗ claudePath 없다: …` · exit 1. 키 자체가 없으면 `· claudePath 없음 — 맥 · 리눅스는 SDK 동봉 CLI 를 쓴다`(✗ 아님, 설계대로) | 사본에서 설정 넷으로 직접 |
| **3** | `docs/INSTALL-WINDOWS.md` | **○** 번호 걸음 **12**(≥ 6) · "친다 · 확인" 줄 28 · 코드 블록 안 줄 끝 백틱 0(이어쓰기 없음, 명령은 한 줄 인라인) · `(Get-Command claude).Source` 절대 경로 · `settings.local.json` 자리와 허용 수 확인 · 작업판 신뢰(8번) · 마지막 12번 = 브라우저로 판 셋 눌러 보고 meta 에 적어 줌(N13) | grep · awk |
| 4 | `smoke/m4-sessions.mjs` (맥 · haiku · 셋 · 5분) | **○** `RSS_MB` 0~5 여섯 줄 · 서버 **최대 91.2 MB**(≤ 300) · 자식 셋 합 **최대 824.1 MB**(≤ 1.5 GB) · `CHILD_PROCS 3` 내내 · `STATES` 셋 idle(error 0) · `STOP_API` 셋 200 · `ASKED []` · $0.09 · exit 0 | `m4m/m4.log` |
| 5 | as-built 절 여섯 | **○** 1 폴더 나무 · 2 표 둘 · 3 API · 4 시험 묶음과 건수 · 5 설계와 다르게 된 자리 · 6 알고 두는 것 | 파일 |
| 6 | 윈도우 env 화이트리스트 | **○** `src/session/env.js` 에 `USERPROFILE · APPDATA · LOCALAPPDATA · TEMP · TMP · SystemRoot · ComSpec · CLAUDE_CODE_GIT_BASH_PATH` · 시험 `윈도우 키 여섯(…)이 실리고 그 밖 키는 안 실린다` 통과. 프록시 키(`HTTPS_PROXY` 등)는 없다 — cockpit N14 | grep · `test/sdk-options.test.js` |
| 7 | 문서 정합 | **○** README "쓰는 법" 맥 · PowerShell 나란히 · `docs/log.md` "M4 윈도우 · 설치 · 문서" 절 · VERIFICATION 에 `check.test.js` · `m4-sessions` 줄 | 파일 |
| 8 | 값 표시 주석 | **○** ARCHITECTURE 5.3 "재기동 뒤 값 = 정지 시점 값(바닥) + 새 프로세스 누적" + 도우미 값 포함 여부(SDK 형 정의로 읽음 — 실측 아님이라 적음). as-built 2.3 에 tool_use 요약 "200자 + 줄임표(길면 201자)" | 파일 |

## ② 윈도우 실측 (사람 · 회사 PC) — **미측**
W-1 `npm test` · W-2 `check` · W-3 `m1-hello` · W-4 `m3-restart`. 출력 파일을 `runs/` 옆에 두면 이 기록에 덧붙인다. 건너뛰는 시험의 조건은 as-built 4.1 표(심볼릭 링크 못 만들면 · win32 의 셸 가짜 claude).

## P-M4 예측 대조
| # | 예측 | 실측 | 판정 |
|---|---|---|---|
| P-M4.1 | pass ≥ 154 · fail 0 · skipped 0 | 158 · 0 · 0 | ○ |
| P-M4.2 | ✓ 셋 · 공백 ✗ · 없는 claudePath ✗ | 그대로 | ○ |
| P-M4.3 | 번호 걸음 ≥ 6 · 확인 명령 | 12 · 28 | ○ |
| P-M4.4 | 서버 ≤ 300 MB · 자식 ≤ 1.5 GB · error 0 | 91.2 · 824.1 · 0 | ○ |
| P-M4.5 | 윈도우 npm test fail 0 · 건너뜀 ≤ 5 | 미측(사람) | — |
| P-M4.6 | 윈도우 env 여섯으로 로그인 | 미측(사람 · W1.3) | — |

## 새로 본 것
- 형제 없이 돌린 `npm test` 의 건너뜀이 **14**(cockpit 의 "넷 + 셋 파일" 보다 많다 — 파일 단위 건너뜀이 하위 시험 수로 세어진다). 회사 PC 에서 형제가 없으면 같은 수가 나온다. INSTALL 문서 7번이 형제(prodev)를 만들게 하니 실전 배치에서는 0 이어야 한다.
- `check` 의 `✓ 경로 공백 없음` 은 오류가 하나도 없을 때만 나온다(오류가 있으면 그 줄 대신 ✗ 목록). 설계대로이나 문서의 "셋" 을 "오류 없을 때 셋" 으로 읽어야 한다.
- 자식 프로세스 셋의 상주 메모리 합이 5분 동안 560~824 MB 로 흔들렸다(haiku · idle). 실전 sonnet 셋 · 일하는 중은 이보다 크다 — P-W4.d(≤ 6 GB) 는 실전에서 다시.
- cockpit N14(프록시 env) · N15(윈도우 갈래 첫 실행)는 W1 에서 잰다. 결정은 사람.

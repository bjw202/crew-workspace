# ADR-024 (이름 영어화 · 알림 토큰은 setup 이 받는다) 코드 PR 검수 (2026-09-10 밤)

대상: https://github.com/bjw202/prodev/pull/4 (브랜치 `code/adr-024-english-names`, 커밋 630a5a5 · 54e65bb · 63b9c3d). 10 파일, 160 줄 더하고 44 줄 뺌. 예측 N1~N6 은 `../predictions/prodev-prediction.md`. 관문 아님.
돌리기 전에 prodev 가 물은 것: 산문의 '비서'까지 바꾸느냐 → 식별자만 (한국어 문장 속 일반 낱말은 그대로, `[비서]` 인수인계 표식도 그대로). N1 의 재는 법을 그에 맞춰 분명히 적었다.

| 칸 | 기대 | 실측 (meta 가 사본에서 직접, `2026-09-10-ADR024-code-tests.txt`) | 판정 |
|---|---|---|---|
| N1 식별자 `prodev-<과제>-비서` · `prodev-알림` 잔존 | 0 (옛 ADR 본문 · log 과거 절 제외) | 1줄, `docs/log.md:80` 의 사고 기록(그때 실제 폴더 이름). 제외 대상 → 0 | ○ |
| N2 setup 뒤 `.env` 의 알림 토큰이 실제로 먹힘 | 1/1 | 사례 "그 값이 실제로 먹힌다" 초록 + cron 시험 `author=prodev-notify`. 덮지 않기 사례도 초록 (요구 1건, 들어온 것 2건) | ○ |
| N3 `npm test` · `test:server` | 전부 초록 | 94 · 94 · 0 / 25 · 25 · 0 (23 → 25). 실제 bots/ 안 늘어남 | ○ |
| N4 트리거 20/20 | 20/20 | 이 PR 은 `.claude` · `CLAUDE.md` · `pre-compact.js` 를 바꾸지 않았다 (`git diff --stat` 빈 출력). 분기표가 같으므로 `2026-09-10-ADR022-triggers.md` 의 20/20 이 그대로다 | ○ |
| N5 제작 비용 | ≤ $8 | **$6.14** (13:48~14:02Z) | ○ |
| N6 README 순서대로 토큰 걸음 없이 켜짐 | 1회 성공 | 아직 (다음 실사용 때) | 대기 |
| 새 이름 | bot · notify | setup.js 109행 `prodev-${과제}-bot` · 112행 `prodev-notify` | ○ |
| CLAUDE.md 줄 | ≤ 40 | 25 | ○ |

ADR-024 본문 확인: 맥락에 사람의 세 마디, 결정 둘, `.env` 풀이 한 줄, 알림 로그인만 `fetch`(공용 api 의 쿠키가 봇 등록에 섞이는 것을 막음), 봇 문맥(`settings.json` env)에는 여전히 안 둠(ADR-018 유지), 사람 걸음 셋 → 둘.

**판정: 통과 (N6 은 다음 실사용 때).** 머지 지시. 시험용 봇 폴더 둘(`bots/prodev-worktogether*-비서`)은 옛 이름 그대로 두었다 — 새 과제부터 새 이름.
